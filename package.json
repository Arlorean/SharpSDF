{
  "scripts": {
    "start": "dotnet fable watch src/SharpSDF.fsproj --outDir scripts --run npx vite",
    "console": "dotnet run --project src/SharpSDF.fsproj",
    "postinstall": "dotnet tool restore"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
